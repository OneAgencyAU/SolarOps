Now build the full SolarOps foundation using the secrets already in the Secrets pane. Here is exactly what to build:

TECH STACK:
- Frontend: React + TypeScript + Vite (already scaffolded in /client)
- Backend: Node.js + Express (already scaffolded in /server)
- Database: Supabase (PostgreSQL) — NEVER use Replit's built-in database
- Auth: Firebase Authentication (Google Sign-In only)
- Routing: React Router v6

STEP 1 — Install dependencies:
Frontend: firebase, @supabase/supabase-js, react-router-dom
Backend: @supabase/supabase-js

STEP 2 — Supabase schema:
Create file supabase/migrations/001_initial_schema.sql with this exact SQL:

CREATE TABLE tenants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  slug TEXT UNIQUE NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE users (
  id UUID PRIMARY KEY,
  email TEXT NOT NULL,
  display_name TEXT,
  avatar_url TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE tenant_memberships (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id),
  user_id UUID REFERENCES users(id),
  role TEXT DEFAULT 'agent',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE tenants ENABLE ROW LEVEL SECURITY;
ALTER TABLE users ENABLE ROW LEVEL SECURITY;
ALTER TABLE tenant_memberships ENABLE ROW LEVEL SECURITY;

STEP 3 — Firebase config:
Create client/src/lib/firebase.ts that initialises Firebase using ONLY these env vars:
VITE_FIREBASE_API_KEY, VITE_FIREBASE_AUTH_DOMAIN, VITE_FIREBASE_PROJECT_ID,
VITE_FIREBASE_STORAGE_BUCKET, VITE_FIREBASE_MESSAGING_SENDER_ID, VITE_FIREBASE_APP_ID
Export: app, auth, googleProvider

STEP 4 — Supabase client:
Create client/src/lib/supabase.ts using VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY

STEP 5 — Auth flow:
- /login page: full screen, Google Sign-In button only
- On sign in: check if user exists in Supabase users table
- If new user: insert into users table → redirect to /onboarding
- If existing user with tenant: redirect to /dashboard
- If existing user without tenant: redirect to /onboarding
- Create AuthContext that provides: user, tenant, loading, signOut
- All routes except /login are protected — redirect unauthenticated users to /login

STEP 6 — Onboarding page (/onboarding):
- Single input: "Business name"
- On submit: create tenant (generate slug from name), create tenant_membership (role: admin)
- Redirect to /dashboard

STEP 7 — App shell with sidebar navigation:
Render this layout for all authenticated + onboarded routes:
- Left sidebar with SolarOps logo at top
- Nav items: Dashboard, Voice Agent, Inbox Assistant, Helpdesk, Connections, Settings
- Each links to a placeholder page showing the page name and "Coming soon"
- Top bar: shows tenant name (left) + user avatar + sign out button (right)

STEP 8 — DESIGN (Apple Liquid Glass aesthetic — critical):
- Background: #0a0a0f
- Glass cards/sidebar: background rgba(255,255,255,0.06), backdrop-filter blur(20px), border 1px solid rgba(255,255,255,0.10)
- Primary accent: #4F8EF7 with box-shadow glow on hover
- Text: white primary, rgba(255,255,255,0.6) secondary
- Font: Inter from Google Fonts
- All interactive elements: 200ms ease transitions
- Login page: dramatic full-screen dark background, centered glass card, SolarOps wordmark, Google sign-in button with glass style
- Sidebar active state: glass highlight with blue accent left border
- Subtle radial gradient behind main content (blue tint, very subtle)

STEP 9 — Update replit.md:
Document the full stack, auth flow, database pattern, and add this rule:
"NEVER use Replit's built-in database. Always use Supabase via VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY (frontend) or SUPABASE_SECRET_KEY (backend)."

Build this completely and test that the app runs. Tell me when done.