Replace the entire contents of client/src/pages/LoginPage.tsx with the following. Keep ALL existing auth logic exactly as-is (imports, useAuth, useNavigate, handleGoogleSignIn, supabase calls, loading check) — only the JSX return and styles change.

The key layout fix: left panel uses display:flex, flexDirection:column with explicit flex children so content fills the full height properly — logo at top, hero+stats in middle, cards filling remaining space, testimonial at bottom.

Make these specific changes to the existing file:

1. Find the left panel div (flex: '2 1 0') and change its style to:
   display: 'flex', flexDirection: 'column', padding: '40px 44px', position: 'relative', overflow: 'hidden', flex: '2 1 0'
   Remove justifyContent: 'space-between'

2. Wrap the logo in a div with style={{ position:'relative', zIndex:10, marginBottom:32 }}

3. Wrap the hero+stats block in a div with style={{ position:'relative', zIndex:10 }}

4. Change the card stack container to:
   style={{ 
     position: 'relative', zIndex: 10, 
     flex: '1 1 0',
     minHeight: 0,
     marginTop: 28,
     marginBottom: 28,
     width: '100%',
     filter: 'drop-shadow(0 24px 48px rgba(0,0,0,0.5))'
   }}

5. Inside the card stack, change card positions so they fill the full height of the container:
   - Card 3 (Voice, right): right:'2%', top:'50%', transform:'rotate(4deg) translateY(-50%)', animation uses float3
   - Card 2 (Inbox, middle): left:'50%', top:'45%', transform:'translateX(-50%) rotate(2deg) translateY(-50%)', animation uses float2  
   - Card 1 (Dashboard, left): left:'2%', top:'55%', transform:'rotate(-5deg) translateY(-50%)', animation uses float1
   - All cards width: 260px

6. Add @keyframes float1, float2, float3 to the style tag that animate translateY by -14px at 50% WITHOUT overriding the rotate — use CSS custom property trick or just duplicate the full transform:
   @keyframes float1 {
     0%,100% { transform: rotate(-5deg) translateY(-50%); }
     50% { transform: rotate(-5deg) translateY(calc(-50% - 14px)); }
   }
   @keyframes float2 {
     0%,100% { transform: translateX(-50%) rotate(2deg) translateY(-50%); }
     50% { transform: translateX(-50%) rotate(2deg) translateY(calc(-50% - 10px)); }
   }
   @keyframes float3 {
     0%,100% { transform: rotate(4deg) translateY(-50%); }
     50% { transform: rotate(4deg) translateY(calc(-50% - 16px)); }
   }

7. Wrap the testimonial in a div with style={{ position:'relative', zIndex:10, marginTop:'auto' }} so it always sits at the bottom

Do not change any auth logic, imports, or the right panel. Do not touch any other files.